$id: permission-for-payload.yaml

type: object

properties:
  type:
    enum:
      - read
      - write
  expiresAt:
    type: number
  targetTypeId:
    type: string

required:
  - type
  - expiresAt
  - targetTypeId
