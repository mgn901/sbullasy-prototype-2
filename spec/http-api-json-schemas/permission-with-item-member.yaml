type: object

properties:
  id:
    type: string
  type:
    enum:
      - read
      - write
  expiresAt:
    type: number
  targetType:
    $ref: item-serialized-public.yaml

required:
  - id
  - type
  - expiresAt
  - targetType
