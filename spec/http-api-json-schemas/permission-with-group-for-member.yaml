$id: permission-with-group-for-member.yaml

type: object

properties:
  id:
    type: string
  type:
    enum:
      - read
      - write
  expiresAt:
    type: number
  grantsTo:
    $ref: group-serialized-for-public.yaml

required:
  - id
  - type
  - expiresAt
  - grantsTo
