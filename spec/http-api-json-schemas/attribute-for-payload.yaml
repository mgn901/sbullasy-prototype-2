$id: attribute-for-payload.yaml

type: object

properties:
  value:
    oneOf:
      - type: string
      - type: number
      - type: boolean
      - type: object
        properties:
          id:
            type: string
