$id: permission-with-item-for-member.yaml

type: object

properties:
  id:
    type: string
  type:
    enum:
      - read
      - write
  expiresAt:
    type: number
  targetType:
    $ref: item-serialized-for-public.yaml

required:
  - id
  - type
  - expiresAt
  - targetType
